package optimal_assigner

import (
	"Elev-project/Driver-go/elevator"

	"golang.org/x/text/number"
)

func ElevatorSim(elev Elevator, buttons ) float64{
	// simulates the runtime for one elevator including the new assignment, returns the runtime as a float
	
	return 
}

func OptimalOrderAssigner(numElev int){
	var runTimes [numElev]float64
	for i := 0; i < numElev; i++{
		for j := 0; j < numElev; j++{
			
		}
	}
}